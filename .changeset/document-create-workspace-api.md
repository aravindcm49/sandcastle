---
"@ai-hero/sandcastle": patch
---

Update README sandbox providers table and intro text to reflect that workspace methods (`ws.run()`, `ws.interactive()`, `ws.createSandbox()`) accept the same providers as top-level functions, and that `noSandbox` is accepted by `ws.interactive()` (as its default).
